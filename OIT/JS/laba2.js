function First(){
    let table = '<table class="table">';
    let rows = prompt("Введите кол-во столбцов");
    rows = Number.parseInt(rows);
    for(let i=0;i<=rows;i++){
        
    }
    table += '</table>';
    
}